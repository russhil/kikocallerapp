// CustomPopup receives title/message/buttons as props (callers pass already-translated
// text). The only hardcoded default — the fallback 'OK' button — reuses common.ok, so
// there are no popup-specific strings to translate.
const popup = {};

export default popup;
